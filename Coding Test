q1 : I am a beginner in this field, but with training I will improve and develop. Question number two, I solved it because I learned JavaScript.

q2 : 
function missingNumber(nums) {
    const n = nums.length;
    const expectedSum = (n * (n + 1)) / 2;
    let actualSum = 0;
    for (let num of nums) {
        actualSum += num;
    }
    return expectedSum - actualSum;
}
